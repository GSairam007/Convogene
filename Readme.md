In amd_backend_rag

For Devbuild_Final_Until_Now.yml
Add the DOCKER_USERNAME and DOCKER_PASSWORD into ur GitHub repository secrets

For the tags based on the username and version



In amd-frontend-code

For Devfront_Final_Until_Now.yml
Add the DOCKER_USERNAME and DOCKER_PASSWORD into ur GitHub repository secrets

For the tags based on the username and version



In Convogene folder

For docker-compose.yml file
VM_HOST         # VM IP Address e.g. 192.168.1.100
VM_USER         # username
VM_SSH_PRIVATE_KEY
VM_PORT         # default is 22
PROJECT_DIR      # The folder path on your VM where you cloned the repo e.g. /home/ubuntu/amd-project